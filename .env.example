# bright-dark-mode-app — variables d'environnement
# Copier ce fichier en .env puis ajuster les valeurs : cp .env.example .env

# URL du backend VoltCompute (FastAPI). En local : uvicorn sur le port 8000.
VITE_API_URL="http://localhost:8000"
